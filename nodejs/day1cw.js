const customerName:" Nakul ";
const customerReview:" The cafe is very modern";
const review={
    name:customerName
    review:customerReview
};
function displayDetails(reviewObj){
    const upperName=reviewObj.name.toUpperCase();
    const 
}